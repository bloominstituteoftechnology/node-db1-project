/***********************************************************
  messages
***********************************************************/

const { message } = require ('../')

/***************************************
  MAIN
***************************************/

const messages = {
  'hello' :
    () => (ri, ro) => (message (
      `hello! i'm ~${ri.originalUrl}.`
    )),
}

/**************************************/

module.exports = messages
